apiVersion: v1
kind: Service
metadata:
  name: node-app-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: node-app
    version: blue   # 👈 control traffic here
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30008